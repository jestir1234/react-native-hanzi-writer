# Project-local installs only (avoid npm install -g for this repo).
# Example app: run `npm run example:start` from repo root — it cds into example/ so Expo
# uses example/package.json (not the library root, which points main at lib/commonjs).
fund=false
